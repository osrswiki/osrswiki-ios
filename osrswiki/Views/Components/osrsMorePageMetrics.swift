import SwiftUI

/// Shared More-page density. About, Feedback, and Donate inherit these
/// instead of per-label padding hacks.
enum osrsMorePageMetrics {
    static let pageStackSpacing: CGFloat = 16
    static let sectionSpacing: CGFloat = 12
    static let creditBlockSpacing: CGFloat = 8
    static let headingBodySpacing: CGFloat = 4
    static let horizontalPadding: CGFloat = 24
    static let verticalPadding: CGFloat = 16
    static let donateWikiBlockSpacing: CGFloat = 32
}
